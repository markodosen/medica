@model IEnumerable<Medica.Models.RadnoVrijeme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SatiOd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SatiDo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SatiOd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SatiDo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RadnoVrijemeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RadnoVrijemeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RadnoVrijemeID })
        </td>
    </tr>
}

</table>
